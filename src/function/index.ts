const SymbolID = Symbol("VariableError");
const ArrayID = Symbol("Array");
const ObjectID = Symbol("Object");
const SplitTextID = Symbol("SplitText");
const BufferID = Symbol("Buffer");
const HttpID = Symbol("HTTP");
const InlineKeyboardID = Symbol("InlineKeyboard");
const KeyboardID = Symbol("Keyboard");
const KeyboardOptionsID = Symbol("KeyboardOptions");
const ReplyParametersID = Symbol("ReplyParameters");
const FileID = Symbol("File");
const FileAnswerID = Symbol("FileAnswer");
const RandomID = Symbol("RandomID");

export {
  SymbolID,
  ArrayID,
  ObjectID,
  SplitTextID,
  BufferID,
  HttpID,
  InlineKeyboardID,
  KeyboardID,
  KeyboardOptionsID,
  ReplyParametersID,
  FileID,
  FileAnswerID,
  RandomID,
};
